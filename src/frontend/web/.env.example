# Supabase — projeto do painel. A chave anon é pública (a RLS do banco protege os dados).
# NUNCA coloque aqui a service_role.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Base da API usada para criar usuarios / associar tecnico (opcional).
# Em branco = mesma origem em producao, http://localhost:3000 em dev.
VITE_API_BASE_URL=
